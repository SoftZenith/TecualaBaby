@page
@model TecualaBaby.Pages.Competencia.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.competencia[0].IdTipoCompetencia)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.competencia[0].Nombre)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.competencia) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoCompetencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdCompetencia">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdCompetencia">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdCompetencia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
